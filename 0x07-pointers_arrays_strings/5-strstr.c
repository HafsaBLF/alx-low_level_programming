#include <stdio.h>
#include "main.h"
#include <string.h>
/**
 **_strstr - locate a substring..
 *@haystack: pointer
 *@needle: pointer
 *Return: new line
 */
char *_strstr(char *haystack, char *needle)
{
int x, y;
for (x = 0; x < 8; x++)
{
for (y = 0; y < 8; y++)
{
return (a[x][y]);
}
return ('\0');
}
}
