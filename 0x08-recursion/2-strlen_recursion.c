#include <stdio.h>
#include "main.h"
#include <string.h>
/**
 * _strlen_recursion - return the length of a string
 * @s: string
 *return: length of string
 */
int _strlen_recursion(char *s)
{
int l;
l = strlen(s);
return (l);
}
