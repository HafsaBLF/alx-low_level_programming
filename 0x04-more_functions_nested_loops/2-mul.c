#include <stdio.h>
#include "main.h"
/**
 * mul - entry point
 * @a: a is a digit
 * @b: b is a digit
 * Return: 1 if c is a digit, 0 otherwise
 */
int mul(int a, int b)
{
int res;
res = a * b;
return (res);
}
